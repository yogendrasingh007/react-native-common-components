import React from 'react';
import { StatusBar, View, Text, ImageBackground } from 'react-native';
import { BaseComponent, Button } from '#CommonComponents';

export default class MoreScene extends BaseComponent {
	render() {
		return (
			<View>
				<Text>more</Text>
			</View>
		);
	}
}
